<UserControl x:Class="surtr.MainModule.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Expander Header="Configuration"  IsExpanded="False">
            <StackPanel>
            <GroupBox Header="Library folder">
                <Label Content="{Binding LibraryFolder}"></Label>
            </GroupBox>
                <Button Command="{Binding LoadCommand}">Load</Button>
            </StackPanel>
        </Expander>
        <Expander Header="Library">
            <ScrollViewer>
                <DataGrid></DataGrid>
            </ScrollViewer>
        </Expander>
        <Button Command="{Binding SynchronizeCommand}">Synchronize</Button>
    </StackPanel>
</UserControl>
